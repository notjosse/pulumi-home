name: ecs-flask-app
runtime:
  name: python
  options:
    virtualenv: venv
description: A Python program to deploy a simple Flask app on AWS ECS
config:
  pulumi:tags:
    value:
      pulumi:template: ""
