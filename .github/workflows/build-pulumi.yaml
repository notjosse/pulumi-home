name: Build Pulumi

on:
  # push:
  #   branches:
  #     - develop
  # with Pulumi GH App Off
  pull_request:
    branches:
      - main

jobs:
  call-pulumi-workflow:
    uses: notjosse/pulumi-home/.github/workflows/pulumi.yaml@develop
    with:
      stack-name: dev
    secrets:
      PULUMI_ACCESS_TOKEN: ${{ secrets.PULUMI_ACCESS_TOKEN }}