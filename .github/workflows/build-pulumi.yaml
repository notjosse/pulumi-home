name: Build Pulumi

on:
  push:
    branches:
      - develop
  pull_request:
    branches:
      - main

jobs:
  call-pulumi-workflow:
    uses: notjosse/pulumi-home/.github/workflows/pulumi.yaml@develop
    with:
      stack-name: dev